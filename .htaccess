RewriteEngine On

# Si existe archivo o carpeta, servir directamente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Enviar todo al front controller en raíz
RewriteRule ^ index.php [QSA,L]
